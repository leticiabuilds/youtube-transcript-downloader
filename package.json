{
  "name": "youtube-transcript-downloader",
  "private": true,
  "packageManager": "npm@11.6.2",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "setup:api": "node scripts/setup-api.mjs",
    "local-dev": "npm run setup:api && turbo run dev",
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint"
  },
  "devDependencies": {
    "turbo": "^2.5.4"
  }
}
